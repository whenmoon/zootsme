import firebase from 'firebase';
// import config from '../config/config'

  // firebase.initializeApp(config);

  // export const db = firebase.database();

  // export const writePhotoDataToDatabase = (uuid, emailAddress, imageUrl, voteCount) => {
  //   db().ref('users/' + emailAddress).set({
  //     uuid: uuid,
  //     email: emailAddress,
  //     imageUrl: imageUrl,
  //     voteCount: voteCount
  //   });
  // }
